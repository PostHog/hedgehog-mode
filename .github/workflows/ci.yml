name: Build

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        name: Install pnpm
        with:
          run_install: false

      - name: Use Node.js from .nvmrc
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          registry-url: https://npm.pkg.github.com/
          node-version-file: .nvmrc
          scope: "@posthog"

      - name: Install dependencies and run checks
        run: |
          pnpm install
          pnpm lint
          pnpm format:check
          pnpm build
          pnpm test

      # Chromium only, and only the browser this suite launches.
      - name: Install Playwright chromium
        run: pnpm --dir e2e run test:install

      # Runs the engine in a real browser under a CSP with no unsafe-eval. Nothing else in
      # this pipeline can catch pixi reaching for `new Function` at shader-compile time.
      - name: Run CSP browser tests
        run: pnpm test:e2e
