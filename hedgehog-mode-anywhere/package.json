{
  "name": "hedgehog-mode-anywhere",
  "version": "2.1.0",
  "private": true,
  "description": "Add an adorable animated hedgehog buddy to any website, powered by PostHog's hedgehog-mode engine.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch",
    "lint:firefox": "pnpm build && web-ext lint --source-dir artifacts/firefox",
    "run:firefox": "pnpm build && web-ext run --source-dir artifacts/firefox",
    "package:chrome": "pnpm build && web-ext build --source-dir artifacts/chrome --artifacts-dir artifacts --filename hedgehog-mode-chrome.zip --overwrite-dest",
    "package:firefox": "pnpm build && web-ext build --source-dir artifacts/firefox --artifacts-dir artifacts --filename hedgehog-mode-firefox.zip --overwrite-dest"
  },
  "dependencies": {
    "@posthog/hedgehog-mode": "workspace:*",
    "pixi.js": "^8.19.0",
    "react": "^18.3.1 || ^19",
    "react-dom": "^18.3.1 || ^19"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "esbuild": "^0.25.12",
    "typescript": "^6.0.3",
    "web-ext": "10.5.0"
  },
  "comment": "Depends on the workspace @posthog/hedgehog-mode, which must keep pixi.js externalized (PostHog/hedgehog-mode#30): the inlined-pixi builds can't be patched for the eval/worker CSP limits of MV3 content scripts. pixi.js is a direct dep because content.jsx imports pixi.js/unsafe-eval and loadTextures."
}
